# 詳細設計書

> **パラメータ**: `$ARGUMENTS` に `identifier` と `説明` を指定してください
>
> - 形式: `/design <identifier> <説明>`
> - 例: `/design SMP-123 ユーザー管理機能`
>
> **指示**: このプロンプトは詳細設計書作成のみを目的としています。
>
> - コードの実装・修正は行わないでください
> - コードベースを調査し、以下のテンプレートを埋めてください
> - `{...}` のプレースホルダーを具体的な内容に置き換えてください
> - 該当しないセクションは削除してください
> - `$ARGUMENTS` の最初の単語を `identifier` として扱い、それ以降を `説明` として扱ってください
> - 完成した詳細設計書は `spec/<identifier>/design.md` に保存してください

---

## 参照

#file:spec/{identifier}/requirement.md

---

## 1. 処理フロー

### シーケンス図

```mermaid
sequenceDiagram
    participant User
    participant Frontend
    participant API
    participant DB
    {シーケンス図の内容}
```

### 処理ステップ

| No. | 処理名   | 処理内容         | 入力         | 出力         |
| --- | -------- | ---------------- | ------------ | ------------ |
| 1   | {処理名} | {処理の詳細説明} | {入力データ} | {出力データ} |

---

## 2. データ設計（該当する場合）

### テーブル定義

#### {テーブル名}

| カラム名   | 型         | NULL | PK  | 説明   |
| ---------- | ---------- | ---- | --- | ------ |
| {カラム名} | {データ型} | {-}  | {○} | {説明} |

### ER図

```mermaid
erDiagram
    {ER図の内容}
```

---

## 3. API設計（該当する場合）

### エンドポイント一覧

| Method | パス       | 概要       | 認証 |
| ------ | ---------- | ---------- | ---- |
| {GET}  | {/api/xxx} | {API概要}  | {要} |

### {API名}

**リクエスト**

```json
{
  "field1": "{説明}"
}
```

**レスポンス**

```json
{
  "field1": "{説明}"
}
```

---

## 4. 画面設計（該当する場合）

### 画面一覧

| 画面名   | 概要         | 遷移元   | 遷移先   |
| -------- | ------------ | -------- | -------- |
| {画面名} | {画面の概要} | {遷移元} | {遷移先} |

### 主要画面レイアウト

```
{画面レイアウトの説明}
```

---

## 5. 実装方針

### 技術的アプローチ

- {アプローチ1}
- {アプローチ2}

### 注意点・制約

- {注意点1}
- {制約1}

---

## 6. 次のステップ

> この詳細設計書の作成後、以下のコマンドでタスク分割に進んでください:
>
> ```
> /tasks <identifier>
> ```
